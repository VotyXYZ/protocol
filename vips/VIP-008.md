# VIP-008: Document extra data

This VIP defines a way to store custom data in documents.

Every type of document contains a field called `extra`:

```ts
type WithExtra<T extends object> = T & { extra: object };
```
